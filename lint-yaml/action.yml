---
name: Lint YAML
description: Lints every YAML file
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - yamllint
    - disable-line
    - rule:truthy
    - ./
outputs:
  hints:
    description: 'List of YAML lint findings'
